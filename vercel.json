{
  "buildCommand": "npm install --prefer-offline && chmod -R 755 node_modules/.bin && npx vite build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
