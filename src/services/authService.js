// This file exists only so that if a stale copy of it ever reappears on
// disk, it still works: it just re-exports everything from the real
// implementation in authService.ts, so it can never fall out of sync.
export * from './authService.ts'